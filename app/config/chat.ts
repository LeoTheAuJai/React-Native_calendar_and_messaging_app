// app/config/chat.ts
export const chatConfig = {
  apiKey: 'qauhxd7xsspd',
  // Remove hardcoded userId and userName
  // These will be set dynamically after login
};

// Default user for initial testing (optional)
export const defaultUser = {
  userId: 'leo31',
  userName: 'Leo',
};

